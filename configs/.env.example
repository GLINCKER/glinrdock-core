# Admin authentication token for write operations
ADMIN_TOKEN=your-strong-token-here

# Data directory for SQLite database and storage
DATA_DIR=/var/lib/glinrdock

# HTTP server listen address
HTTP_ADDR=:8080

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# CORS allowed origins (comma-separated)
GLINRDOCK_CORS_ORIGINS=

# Plan Configuration
# Available plans: FREE (default), PRO, PREMIUM
GLINRDOCK_PLAN=FREE

# Plan Limit Overrides (optional)
# Free plan limits (defaults: 3 tokens, 2 clients)
FREE_MAX_TOKENS=3
FREE_MAX_CLIENTS=2

# Pro plan limits (defaults: 10 tokens, 10 clients)
PRO_MAX_TOKENS=10
PRO_MAX_CLIENTS=10

# Premium plan has unlimited tokens and clients

# Example configurations by use case:
#
# Individual Developer (Free Plan):
# GLINRDOCK_PLAN=FREE
# ADMIN_TOKEN=dev-secure-token
#
# Small Team (Pro Plan):
# GLINRDOCK_PLAN=PRO  
# ADMIN_TOKEN=team-secure-token
#
# Enterprise (Premium Plan):
# GLINRDOCK_PLAN=PREMIUM
# ADMIN_TOKEN=enterprise-ultra-secure-token