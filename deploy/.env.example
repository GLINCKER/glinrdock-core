# GlinrDock Docker Compose Configuration
# Copy this file to .env and customize the values

# Domain and SSL
DOMAIN=your-domain.com
EMAIL=admin@your-domain.com

# Core GlinrDock settings
ADMIN_TOKEN=your-super-secure-admin-token-change-this
WEBHOOK_SECRET=your-webhook-hmac-secret

# Data directory (local path for bind mount)
DATA_DIR=./data

# Database settings (for PostgreSQL profile)
POSTGRES_DB=glinrdock
POSTGRES_USER=glinrdock
POSTGRES_PASSWORD=your-secure-database-password

# Redis settings (for Redis profile)
REDIS_PASSWORD=your-secure-redis-password

# Monitoring settings (for monitoring profile)
GRAFANA_PASSWORD=your-secure-grafana-password

# Optional: Custom Docker registry
# REGISTRY_URL=ghcr.io/yourorg/glinrdock
# REGISTRY_TAG=latest